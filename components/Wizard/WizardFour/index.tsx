const index = ({
  step,
  setStep,
  service,
  setService,
} : {
  step: number,
  setStep: (e: number) => void,
  service,
  setService: (s: string) => void,
}) => (
  <div>
    <p>four</p>
  </div>
);

export default index;
