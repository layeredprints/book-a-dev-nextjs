const index = ({
  step,
  setStep,
  setService,
} : {
  step: number,
  setStep: (e: number) => void,
  setService: (s: string) => void,
}) => (
  <div>
    <p>one</p>
  </div>
);

export default index;
